/* Custom positioning for top center and bottom center logos */
.reveal .top-center-logo {
    position: fixed;
    top: 0;
    left: 30%;
    transform: translateX(-50%);
    height: 50px; /* Adjust as needed */
    padding: 5px;
    z-index: 1000; /* Ensure it is above other content */
}

.reveal .bottom-center-logo {
    position: fixed;
    bottom: 0; /* Stick to the bottom edge */
    left: 50%;
    transform: translateX(-50%);
    height: 30px; /* Adjust as needed */
    padding: 5px;
    z-index: 1000; /* Ensure it is above other content */
}

/* Center the image */
.figure-large {
  width: 80%; /* Adjust as needed */
  height: auto; /* Maintain aspect ratio */
  display: block; /* Block-level element */
  margin: 0 auto; /* Center horizontally */
  position: relative; /* Allows positioning of child elements */
}

/* Style for animated text */
.animated-text {
  font-size: 16px;
  color: black; /* Text color */
  opacity: 0;
  transition: opacity 2s ease-in-out;
}

/* Fade in effect for the animated text */
.fade-in {
  opacity: 1;
}

/* Ensure top padding to avoid overlap with top logo */
.reveal .slides > section {
    padding-top: 120px; /* Increase if necessary */
    padding-bottom: 120px; /* Increase if necessary */
}

/* Ensure bottom padding to avoid overlap with bottom logo */
.reveal .slides > section::after {
    content: "";
    display: block;
    height: 100px; /* Adjust this value as needed */
    background: url('images/bottom-background.png') no-repeat bottom center; /* Update with your image path */
    background-size: cover;
    position: absolute;
    bottom: 0;
    left: 0;
    right: 0;
    z-index: -1; /* Ensure it is behind content */
}

/* Style for the figure container to center the image */
.figure-container {
  text-align: center; /* Center-aligns the content within the div */
  margin: 0 auto; /* Centers the div itself */
}
